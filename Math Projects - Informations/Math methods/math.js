let x = 3;
let y = 2;
let z = 1;

//z = Math.round(x);    <= Rundet die Zahl auf die nächstgelegene Ganzzahl
//z = Math.floor(x);    <= Rundet die Zahl auf die nächstgelegene kleinere Ganzzahl
//z = Math.ceil(x);     <= Rundet die Zahl auf die nächstgelegene größere Ganzzahl
//z = Math.trunc(x);    <= Entfernt alle Dezimalstellen und gibt die Ganzzahl zurück, ohne zu runden
//z = Math.pow(x, y);   <= Berechnet die Potenz
//z = Math.sqrt(x);     <= Berechnet die Quadratwurzel
// z Math.log(x);       <= Berechnet den natürlichen Logarithmus
//z = Math.sin(x);      <= Berechnet den Sinus
//z = Math.cos(x);      <= Berechnet den Kosinus
//z = Math.tan(x);      <= Berechnet den Tangens
//z = Math.abs(x);      <= Gibt den absoluten Wert zurück
//z = Math.sign(x)      <= Gibt das Vorzeichen zurück
let max = Math.max(x, y, z);
let min = Math.min(x, y, z);

console.log(min);